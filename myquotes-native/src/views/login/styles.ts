import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    color: "green",
    backgroundColor: "white",
    fontWeight: "normal",
    fontSize: 18,
    height: 40,
    textAlign: "center",
  },
});
